export const DEFAULT_PORT = 3000;
export const HTTP_STATUS = {
  INTERNAL_SERVER_ERROR: 500,
};

export const DIARY_COLUMNS = {
  TIME: 0,
  PET_NAME: 1,
  ACTION: 2,
  DESCRIPTION: 3,
};

export const PET_COLUMNS = {
  NAME: 0,
  TYPE: 1,
};

export const ACTION_COLUMNS = {
  NAME: 0,
};

export const UI_LIMITS = {
  MAX_DIARY_QUERY: 10,
};

export const DATE_FORMAT_LENGTHS = {
  ISO_DATE: 10, // YYYY-MM-DD
  TIME_HH_MM: 5, // HH:mm
};
